function lowerCaseDrivers(drivers){
  let lowerDrivers = drivers.map( driver => driver.toLowerCase());
  return lowerDrivers;
};

function nameToAttributes(drivers){
  const splitDrivers = drivers.map(driver =>);
};

function attributesToPhrase(){};
