function lowerCaseDrivers(drivers){
  const lowerDrivers = drivers.map( driver => driver.toLowerCase());
  return lowerDrivers;
};

function nameToAttributes(drivers){
  splitDrivers = drivers.map(driver => driver.split(' '))
};

function attributesToPhrase(){};
